name: Run Managed Agent

# Browser-only runner for a Hawkeye Managed Agent session.
#
# Trigger from: GitHub → Actions → Run Managed Agent → Run workflow.
# Pick the agent, type your kickoff message, click "Run workflow".
#
# Required secrets:
#   ANTHROPIC_API_KEY     — console.anthropic.com
#   HAWKEYE_BRAIN_TOKEN   — same value that's set in Netlify env vars
#
# Optional secrets:
#   HAWKEYE_BRAIN_URL     — defaults to https://hawkeye-sterling-v2.netlify.app
#   CACHET_BASE_URL       — if you want Cachet publishing
#   CACHET_API_TOKEN      — pair with CACHET_BASE_URL
#
# The workflow downloads the .env.agents artifact produced by the most
# recent successful "Install Managed Agents" run. Install the agents
# first, then run this workflow.

on:
  workflow_dispatch:
    inputs:
      agent:
        description: Which agent to run
        required: true
        default: incident-commander
        type: choice
        options:
          - incident-commander
          - hawkeye-mlro
      message:
        description: Kickoff message for the agent
        required: true
        default: "Smoke test. Call brain_event with kind=manual, severity=info, summary=github actions smoke test. Then summarize the response."
        type: string

permissions:
  contents: read
  actions: read

jobs:
  run:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      # No artifact download — the orchestrator now looks up agent ids
      # by NAME via the Anthropic API when .env.agents is missing, so
      # we don't have to ferry the file between workflows.

      - name: Run agent orchestrator
        id: run
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          HAWKEYE_BRAIN_URL: ${{ secrets.HAWKEYE_BRAIN_URL || 'https://hawkeye-sterling-v2.netlify.app' }}
          HAWKEYE_BRAIN_TOKEN: ${{ secrets.HAWKEYE_BRAIN_TOKEN }}
          CACHET_BASE_URL: ${{ secrets.CACHET_BASE_URL }}
          CACHET_API_TOKEN: ${{ secrets.CACHET_API_TOKEN }}
          # Pass the user-supplied values via env so shell quoting is
          # not an issue when the kickoff message contains quotes or
          # apostrophes. Bash variable expansion inside double quotes
          # handles these correctly; ${{ inputs.message }} interpolated
          # directly into the command line does NOT.
          AGENT_SLUG: ${{ inputs.agent }}
          AGENT_KICKOFF: ${{ inputs.message }}
        run: |
          set -o pipefail
          mkdir -p agent-runs
          LOG="agent-runs/${AGENT_SLUG}-$(date +%Y%m%dT%H%M%S).log"
          node scripts/agent-orchestrator.mjs "$AGENT_SLUG" "$AGENT_KICKOFF" 2>&1 | tee "$LOG"
          echo "log=$LOG" >> "$GITHUB_OUTPUT"

      - name: Upload run log
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: agent-run-${{ inputs.agent }}-${{ github.run_number }}
          path: agent-runs/
          retention-days: 30

      - name: Write step summary
        if: always()
        env:
          AGENT_SLUG: ${{ inputs.agent }}
          AGENT_KICKOFF: ${{ inputs.message }}
          RUN_OUTCOME: ${{ steps.run.outcome }}
        run: |
          {
            echo "# Hawkeye agent run"
            echo
            echo "**Agent:** \`$AGENT_SLUG\`"
            echo "**Outcome:** \`$RUN_OUTCOME\`"
            echo
            echo "**Kickoff:**"
            echo
            printf '> %s\n' "$AGENT_KICKOFF"
            echo
            if [ "$RUN_OUTCOME" = "failure" ]; then
              echo "## :x: Run failed — inline log"
              echo
              LATEST_LOG=$(ls -t agent-runs/*.log 2>/dev/null | head -n1 || true)
              if [ -n "$LATEST_LOG" ] && [ -f "$LATEST_LOG" ]; then
                echo '```'
                # Tail the last 200 lines so the summary doesn't blow
                # up on long streaming transcripts.
                tail -n 200 "$LATEST_LOG"
                echo '```'
              else
                echo "_no log file produced_"
              fi
              echo
            fi
            echo "Full transcript artifact: \`agent-run-$AGENT_SLUG-${{ github.run_number }}\`"
          } >> "$GITHUB_STEP_SUMMARY"
